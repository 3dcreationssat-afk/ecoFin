# Import Format

Transaction import is not implemented in this release.

Planned import support will document CSV columns, date handling, sign conventions, duplicate handling, category mapping, merchant normalization, and validation failures before import is enabled.

No real CSV, OFX, QFX, QBO, PDF statement, or financial export files should be committed.

